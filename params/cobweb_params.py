# Default parameters for the cobweb diagram
import numpy as np

# number_iterations : Number of iterations considered for the cobweb
number_iterations   = 1000
# r                 : the growth parameter used
r                   = np.float64(3.99)
# x_seed            : the initial seed
x_seed              = np.float64(0.5)
